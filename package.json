{
  "name": "fast-food-auth-lambda",
  "version": "0.1.0",
  "private": true,
  "main": "dist/bootstrap.js",  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/bootstrap.js",
    "test": "jest --coverage",
    "lint": "eslint 'src/**/*.{ts,tsx}' --ext .ts",
    "format": "prettier --write .",
    "package": "npm run build && cd deploy && zip -r fast-food-auth-lambda.zip ."
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/node": "^20.5.1",
    "@types/aws-lambda": "^8.10.119",
    "eslint": "^8.0.0",
    "jest": "^29.6.1",    "ts-jest": "^29.1.0",
    "typescript": "^5.5.6",
    "prettier": "^2.8.8"
  }
}
